# Original task prompt

This is the task exactly as given to the agent for this port, verbatim:

> port all config management functions from svaichu/robotdataset, check all
> branches.
>
> I want it to work with optuna.
> Add examples, documentation.
> Also create a agent dir with your plan (also prompt) and thinking.
> process.

Working repo: `svaichu/training_cfgs`, branch
`claude/config-mgmt-robotdataset-port-mu3rcq`.

Source repo: `svaichu/robotdataset` (added read-only for this task via
`add_repo` / cloned to `/workspace/robotdataset`).
